# Dev/test tooling only (F-07). CI installs both files; production installs
# requirements.txt alone. Pinned exact — regenerate from a working venv:
#   pip freeze | grep -iE "^(pytest|httpx|ruff|mypy|playwright)"
pytest==9.1.1
httpx==0.28.1
ruff==0.16.5
mypy==2.3.1
playwright==1.62.0
